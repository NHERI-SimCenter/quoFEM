filters: ~

workflows:
  tests:
    report:

  examples-gallery:
    # construct html page for filtered example gallery
    report:
      title: quoFEM
      template: rprt-0001
      template_data:
          gallery_items:
              src/input.json:
                UQ: UQ_Method.uqType
                FEM: fem.program
              meta.yaml:
                title: title
                synopsis: abstract
                Series: collection-title
                src-url: URL

          filters: [UQ, FEM, Series]



